project:
  name: { { PROJECT_NAME } }
  version: 1.0.0
  description: { { DESCRIPTION } }
  stack: []

paths:
  # CRITICAL: This explicit relative path enforces that the agent writes OUTSIDE the totem/ history directory.
  # Adjust the dots depending on where the `project-code` folder is located relative to the `totem/instances/` folder.
  code: ../../../../{{PROJECT_NAME}}
  history: .

guardians:
  default:
    - ROOT
    - PM
    - QA
    - DEVOPS
    - ARCHITECT
  stack_adapters: [] # List of specific .ti files from /stacks (e.g. react/REACT)
  
  # UI Reference for GUI Orchestrator
  ui_stack_ref: react/REACT # Default UI reference for dashboard generation
