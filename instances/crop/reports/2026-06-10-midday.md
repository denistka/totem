# Midday status — Wed 2026-06-10

**Author:** @denistka
**Focus:** PR #1724 brand hub merge · crop-dev QA PASS on 7 Oleg PRs

---

## ✅ Shipped

Brand hub parity is now live on dev — the unified template that all four new brands share is merged and verified.

**4 items shipped:**

- **PR #1724 merged** — brand hub parity (scopes A+B) landed on `dev` at 11:08 MSK. Vova approved after Denis rebased onto current dev (conflict in the features/brands barrel file) and dropped the 2 animation commits Vova flagged: the hero peel animated the identity element that template simplification (#1737) is about to remove, so shipping and immediately deleting it made no sense. Rebase + drop verified clean: typecheck, biome lint, knip dead-export check, 316 unit tests — all green. Denis ran crop-dev PASS at 375px and 1280px across all 5 brand hubs (NH, Ferris, Ventrac, McHale, Marcrest).
  - #1703 brand hub parity — closed — https://github.com/CT-CROP/CROP-front/issues/1703

- **crop-dev PASS: Header Brands dropdown** — Oleg's global nav upgrade (#1692) works correctly. The header shows a "Brands" dropdown with all 5 brands; the mobile sidebar shows a "Shop by Brand" section with the same list; "View all brands" routes to `/brands`. Tested at 375px and 1280px.
  - #1664 header brands nav — auto-closed on merge — https://github.com/CT-CROP/CROP-front/issues/1664

- **crop-dev PASS: GroupNav scroll-spy** (#1693) — on the Ferris parts-diagrams page (`crop-dev.app/parts-diagrams/ferris`), the equipment-group navigation bar correctly highlights the topmost visible section as the user scrolls. The URL updates to `#g-{group}` on scroll and the last section stays highlighted at the bottom of the page (tail fix). Tested on crop-dev.
  - #1609 GroupNav scroll-spy — auto-closed on merge — https://github.com/CT-CROP/CROP-front/issues/1609

- **crop-dev PASS: Branded cart SKU** (#1685) — cart items show `FRR-054416ZMA` / `NHL-F775DSW` in the UI, which is the correct display: the cart component intentionally strips the `CT-` prefix for cleaner presentation (`formatDisplaySku` in `cart-item-card.tsx`). The internal `productId` and `sku` stored and sent to Stripe are `CT-FRR-{PN}` and `CT-NHL-{PN}` respectively — confirmed in the unit tests and code. The "invisible plumbing" description in the original issue was accurate.
  - #1662 branded cart SKU — closed — https://github.com/CT-CROP/CROP-front/issues/1662

**Auto-closed on merge (Denis verified working):**
- #1385 My Garage Saved Parts tab — https://github.com/CT-CROP/CROP-front/issues/1385
- #1386 Garage year-range validation removed — https://github.com/CT-CROP/CROP-front/issues/1386
- #1325 slug-less equipment URLs emit `?equipmentModelKey=` — https://github.com/CT-CROP/CROP-front/issues/1325

---

## 🔧 In flight

- **BOM thumbnails (#1695)** — Oleg's frontend plumbing that forwards `imageUrl` to BOM rows is merged and tested. The FE side is correct; the missing piece is on the backend. Daniil's search service currently returns `imageUrl: null` for Ferris/ARI assemblies — thumbnails will appear automatically once that data flows through. #1613 stays open until Daniil's side is deployed.
  - #1613 assembly BOM thumbnails — open, waiting on Daniil — https://github.com/CT-CROP/CROP-front/issues/1613

- **a11y CI gate blocked on dev** — Vova's review on Oleg's PR #1728 (diagrams sticky toolbar) surfaced a pre-existing global issue: the header search combobox has an invalid `aria-controls` value when the dropdown is closed, which fails the `e2e-a11y-smoke` check on the PDP. This was invisible before because #1732 recently unskipped the PDP axe assertion. The toolbar work itself is approved — this is a separate global header component bug that now blocks every PR merging to dev. Fix is small: keep an empty `CommandList` mounted while the input renders so `aria-controls` always resolves.
  - PR #1728 diagrams sticky toolbar — open, blocked by a11y gate — https://github.com/CT-CROP/CROP-front/pull/1728

---

## ⏳ Awaiting

- **Marcrest diagrams (#1538)** — Daniil is working on connecting the 170 Marcrest models to the `/parts-diagrams/marcrest` page. Until that lands, Denis cannot run the Marcrest smoke re-run (A3b). No ETA from Daniil yet.
  - #1538 Marcrest catalog to live diagrams — https://github.com/CT-CROP/CROP-front/issues/1538

- **Template simplification (#1737)** — Oleg owns the hero cleanup: remove the overlapping identity bubble, drop parts-count numbers, remove the serial-number search widget, cut excess copy, and unify section naming per John's 6/9 design meeting. Assigned to Oleg; no PR yet.
  - #1737 brand hub template simplification — https://github.com/CT-CROP/CROP-front/issues/1737

- **Saturday visuals (D1–D3, #1710)** — Oleg owns parts images in the store (D1), category page imagery (D2), the `/brands` card grid (D3), and the parts-diagrams landing redesign (#1710). Content assets (brand marketing photos) are waiting on John. No PRs open yet.

---

## Next

1. **Fix the a11y CI gate** — the header search `aria-controls` bug now blocks all dev PRs. Small frontend fix (~1–2 h). Denis can pick this up to unblock Oleg's #1728 and the upcoming A8 PR.
2. **A8 — NH Construction ≠ NH Agriculture** — the NH Construction brand page currently shows Agriculture categories and imagery (wrong). John flagged this repeatedly. Denis owns the fix; Saturday launch blocker (~2–4 h).
3. **A3b — Marcrest smoke re-run** — after Daniil lands #1538, Denis re-runs the Marcrest parts-diagrams smoke at 375px + 1280px.
4. **crop-dev 375px/1280px full sweep** — Vova does the final visual QA across all brand hubs before D0 deploy.

---

## 👤 Workload

- **Denis** — 2 items (~3–6 h): a11y gate fix (~1–2 h, unblocks everyone), A8 NH Construction fix (~2–4 h). Then A3b smoke after Daniil.
- **Oleg** — 5 items (~14–18 h): #1728 rebase after a11y fix (~1 h), #1737 template simplification (~4–6 h), D1 parts images (~6–8 h), D2 category imagery (~4 h), D3 `/brands` grid (~3 h), #1710 diagrams landing (~4 h). **⚠️ Overloaded — Saturday deadline.**
- **Vova** — 2 items (~3–4 h): coordinate D4 `/parts-diagrams` entry page, 375px/1280px visual QA + D0 deploy (blocks everyone).
- **Daniil** — 1 item (unknown ETA): #1538 Marcrest diagrams data (blocks Denis A3b and Marcrest DoD).
- **John** — marketing assets for Oleg (brand portal photos, popular model list) — no ETA. Blocks D1/D2 visual fidelity.

---

## 💡 Can offload

Oleg holds roughly 60–70% of the remaining Saturday work and is at risk of not finishing in time.

- Denis can take the **a11y gate fix** (header search `aria-controls`) right now — saves Oleg a round-trip with Vova on #1728 and unblocks the CI gate for everyone (~1 h, straightforward ARIA fix, Denis has the repo running locally).
- Denis can pick up **D3 `/brands` card grid** from Oleg if John supplies the brand imagery — it is a layout task (uniform grid, larger cards for top 8 brands) with no Figma-specific design knowledge needed (~3 h off Oleg's plate).
- Denis cannot take D1 (parts images template) or #1737 (template simplification) without Oleg's Figma context and design decisions.
