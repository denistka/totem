gate: OPEN
v:4 role:dev x:frontend
task: Циклические зависимости и упрощение импортов
status: done
requires: [FRONTEND_DEV]

context: |
  Просканировать `navitrack-apps/src-client` (madge, dependency-cruiser, или
  `eslint-plugin-import` no-cycle — выбрать один стек и зафиксировать в отчёте).
  Задокументировать найденные циклы и предложить разрыв (перенос типов,
  промежуточный модуль, инверсия зависимости). Проверить логичность границ
  папок (screens / modules / api / store) и избыточные «ступени» re-export.

  Ограничения: не ломать публичные контракты; правки — минимальные и с тестами
  сборки. Крупный рефактор структуры — только если устраняет зафиксированный цикл.

done:
  - В `S81_DUPLICATION_AND_UNIFICATION.md` (или отдельный раздел) — список
    циклов до/после; либо подтверждение «критичных циклов нет»
  - `npm run build` (или эквивалент) зелёный

out: []
