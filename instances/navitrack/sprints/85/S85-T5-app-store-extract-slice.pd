gate: OPEN
v:4 role:dev x:frontend
task: S85 T5 — extract one useAppStore concern slice
requires: [FRONTEND_DEV]
sprint: S85
dep: [S85-T4-app-store-fanin-audit.pd]

context: |
  Move state + selectors for the slice chosen in T4 into a dedicated module
  (e.g. useHeaderStore or existing pattern). useAppStore re-exports or composes
  only if needed for backward compatibility. No behavior change.

out:
  - Import graph updated; codemap shows lower referencesFromOtherFiles on
    useAppStore.ts OR documented partial with next slice backlog

done:
  - Tests/CI green per project norms; typecheck clean
