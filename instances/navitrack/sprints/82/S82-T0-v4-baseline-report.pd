gate: OPEN
v:4 role:dev x:frontend
task: S82 T0 — deterministic V4 baseline (codemap, jscpd, lint vs immutable seed)
requires: [FRONTEND_DEV]

context: |
  **Seeded doc:** `totem/instances/navitrack/intel/S82_ARCHITECT_V4_BASELINE.md`
  (codemap 377 files, 40 SonarJS warnings, 12 jscpd clones as of 2026-04-08).

  Re-run `pnpm codemap`, `pnpm lint`, `pnpm dupes:jscpd` in src-client. Update
  §1–§3 tables if metrics changed; complete §4 with any new S81/S82 overlap notes;
  append §5 changelog row. Read `S81_DUPLICATION_AND_UNIFICATION.md`. No app
  code in this task unless metrics drift requires a one-line codemap regen only.

out: [totem/instances/navitrack/intel/S82_ARCHITECT_V4_BASELINE.md]

done:
  - Baseline doc refreshed (dates, counts, changelog row) and S81 delta accurate
  - jscpd metrics suitable as `post_sprint_guards` regression baseline (see `S82-ARCHITECT-V4-FULL-OPTIMIZATION.ptl`)
  - UI/UX unchanged constraint restated for downstream tasks
